# Types and data structures for mustache
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

struct MustacheConfig
    name
    prefix
    count
end

function make_config(name = "World", count = 1)
    return MustacheConfig {
        name: name,
        prefix: "Hello",
        count: count
    }
end
